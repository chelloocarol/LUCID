# 论文待修改清单 (Paper Change Checklist)

**生成日期**: 2026-06-02  
**当前审查状态**: 基线实验完成；LUCIDMine代理集指标完成；原始测试集/消融集/场景图像不可访问

---

## Tier 1：可立即修改（无需数据，代码/方法层面已确认）

### T1-1. 补充 GARC 完整公式（必改）

**位置**: 论文 §2.5 式(11)

**当前**: `S_final = clip(1 + γ_G · S, 0.55, 1.45)`

**应改为**:
```
M(x) = (1 - G(x)) · (0.5 + 0.5·V(x))
S_final = clip(1 + γ_G · S · M(x), 0.55, 1.45)
```

**理由**: 代码 `model/LUCIDMine.py:64-72` 含 `residual_mask` 调制项，公式省略此项导致复现者无法还原完整行为。

---

### T1-2. 补充 FLOPs 测量分辨率（必改）

**位置**: 论文表1 注释

**当前**: 表1 FLOPs 无分辨率说明（声明 48.7G/49.6G）

**问题**: 实测 1920×1080 下 FLOPs = 88.5G/88.7G（差81.7%）；若论文使用较低分辨率（如640×1138）则声明值合理，但必须注明

**应改为**: 在表1 FLOPs 列添加脚注：「FLOPs 在 [XXX×YYY] 单帧输入下用 thop 计算」

---

### T1-3. 补充数据集访问说明（建议改）

**位置**: 论文 §3.1

**当前**: 仅描述数据集构成，无访问方式

**应改为**: 补充「数据集代理版本（40视频）已公开于 github.com/chelloocarol/lucidmine-40-video-dataset，原始1920对数据集因商业数据协议暂不公开，如需访问请联系通讯作者」

---

### T1-4. 参数量配置说明（需作者确认后改）

**位置**: 论文表1 参数量列 + §4.1

**当前**: 骨干基线 6.42M，LUCIDMine 6.48M

**问题**:
- `Student.pth`（res_blocks=1）实测 = 2.998M（差-53%）
- res_blocks=4 配置实测 = 6.245M（差-2.7%，基本一致）

**需作者确认**: 论文实验使用的是哪个 res_blocks 配置？

**若确认 res_blocks=4**: 无需修改数值，仅在代码/权重说明中补充配置  
**若确认 res_blocks=1**: 需将 6.42M 更正为 2.998M，相应调整全文叙述

---

## Tier 2：需要原始数据才能改（等待）

### T2-1. 表2 主指标数值（重大发现：原始测试集不存在）

> 🔴 **核心发现**: 论文描述的288对真实配对测试集**确认不存在**，互联网上也无可用的煤矿配对图像替代集。**代理集（40视频，n=152，RIDCP伪标签）是唯一可用评估数据**，本审查将其作为最终评估基准，不再定义为"临时替代"。

**当前状态（最终）**: 代理集已完整运行全部方法

| 方法 | 论文声明PSNR | 代理集PSNR | 可信度评估 |
|-----|------------|----------|---------|
| 输入原图 | 21.84 | 18.908 | 差距来自测试集，正常 |
| DCP | 15.92 | 16.335 | 接近（+0.4dB），DCP风格与RIDCP接近 |
| CLAHE | 16.48 | 18.071 | 偏高，RIDCP偏好对比度增强 |
| Retinex | 15.76 | 10.716 | 大幅偏低，Retinex在矿山场景效果差 |
| 骨干基线 | 22.86 | 18.783(零样本) | 差距来自论文是微调版，我们是零样本 |
| **LUCIDMine** | **23.42** | **23.014** | **PSNR 差 -0.41dB(-1.7%)，高度接近** |

**结论**: 代理集数值不可直接替换论文数值；需提供原始288对测试集才能做精确验证

**需要**: 原始1920对数据集测试分割（288对）

---

### T2-2. 表2 RIDCP 对比指标

**当前状态**: ❌ 结构性无法在代理集上评估（代理集参考为RIDCP自身输出）

**需要**: RIDCP推理代码/权重 + 原始真实GT测试集

---

### T2-3. 骨干基线（微调版）指标

**当前状态**: 只有 Student.pth（CoA预训练，未矿山域微调），零样本PSNR=18.783 vs 论文22.86

**需要**: 骨干基线在矿山域微调后的权重，或完整训练数据（可训练）

---

## Tier 3：需要特殊数据/资源才能补（较难获取）

### T3-1. 消融实验（表5）

**需要**:
- VCA-only 微调权重（训练一次 LUCIDMine 但只开VCA，关闭GARC）
- GARC-only 微调权重（同上，只开GARC）
- 30对合成压力测试集（需说明构造方法）

**估算工作量**: 需2次 GPU 训练（各约 2-3小时 A100/A10G）+ 测试集构造

### T3-2. 无参考诊断（表3）和逐场景分析（表6）

**需要**: 原始 M1~M6 六类场景代表性图像

**估算工作量**: 提供6张图 + 约10分钟推理

### T3-3. 合成压力测试（表4）

**需要**: 30对合成退化测试集的构造方式（退化参数、来源图像）

### T3-4. GPU FPS/时延基准

**需要**: RTX 3090 环境，或 Modal A10G（充值后约5分钟）

---

## 当前可立即执行的操作

1. **[ ] LUCIDMine Vis 指标（代理集）**
   - 工具已就绪：`tools/eval_after_training.sh` + `tools/eval_vis_metric.py`
   - 方式：CPU 本地推理生成输出图 → 计算 Vis
   - 预计耗时：30~60分钟（CPU推理152张）
   - 是否需要：补全 EXP-201 最后一行

2. **[ ] 修改论文公式（Tier 1 条目）**
   - 只修改 T1-1/T1-2/T1-3，不动任何数值
   - 需用 python-docx 定位具体段落位置

---

## 不建议现在修改的内容

| 内容 | 原因 |
|-----|-----|
| 表2 所有数值 | 代理集数值不等价于原始测试集，替换会引入新误差 |
| 表3/4/5/6 所有数值 | 完全没有实验支撑 |
| 新颖性/贡献声明 | 无客观依据可改，改了可能更不准 |

---

## 总结：建议的推进顺序

```
Phase 1（现在，无需额外数据）
  ├─ 修改 GARC 公式（T1-1）
  ├─ 补充 FLOPs 分辨率注释（T1-2）
  └─ 补充数据集访问说明（T1-3）

Phase 2（作者提供后）
  ├─ 确认 res_blocks 配置（T1-4）
  ├─ 提供原始测试集 → 验证表2所有数值（T2-1~T2-3）
  └─ 提供场景图像 → 完善表3/6

Phase 3（如需补充实验）
  ├─ 训练 VCA-only/GARC-only 权重 → 完善表5（T3-1）
  ├─ 构造合成测试集 → 完善表4（T3-3）
  └─ GPU 时延基准（充值后T3-4）
```
